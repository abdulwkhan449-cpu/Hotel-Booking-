// ===== OFFERS PAGE =====
// All interactive features (mobile menu, back to top, etc.)
// are handled by the shared index.js file.

console.log('🏷️ Offers page loaded successfully.');

document.addEventListener('DOMContentLoaded', function() {
  // Any offer-specific interactions can go here.
  // For example, you could add:
  // - Offer countdown timers
  // - Dynamic price calculations
  // - Filter by category
});
