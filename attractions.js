// ===== ATTRACTIONS PAGE =====
// All interactive features (mobile menu, back to top, etc.)
// are handled by the shared index.js file.

console.log('📍 Attractions page loaded successfully.');

document.addEventListener('DOMContentLoaded', function() {
  // Any attraction-specific interactions can go here.
  // For example, you could add:
  // - Interactive map integration
  // - Filter by distance or category
  // - "Get Directions" with live location
});
